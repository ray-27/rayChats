syntax = "proto3";

package pb;

option go_package = "./pb";

service AuthService {
    rpc GetUserData(Token) returns (User);
}

message Token{
    string token = 1;
}

message User {
    string id = 1;
    string name = 2;
    string email = 3;
}